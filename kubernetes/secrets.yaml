apiVersion: v1
kind: Secret
metadata:
  name: chat-secrets
  namespace: chat-app
type: Opaque
stringData:
  # Database Credentials (cambiar en producción)
  POSTGRES_DB: chatdb
  POSTGRES_USER: chatuser
  POSTGRES_PASSWORD: CHANGE_ME_IN_PRODUCTION_12345
  MONGO_DB: chat-messages
  
  # JWT Secret (cambiar en producción)
  JWT_SECRET: CHANGE_THIS_TO_A_SECURE_256_BIT_KEY_IN_PRODUCTION